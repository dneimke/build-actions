{
  "name": "shared",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "libs/Shared",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "outputs": ["{workspaceRoot}/bin/Shared"],
      "options": {
        "command": "npm run build:shared"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "options": {
        "command": "cd libs/Shared && dotnet test"
      }
    }
  },
  "tags": []
} 